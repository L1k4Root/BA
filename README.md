# BA Web

This worktree is for the BA Chile website only.

- Branch: `codex/ba-web`
- App directory: `BA_Web/`
- Package manager: npm, using `BA_Web/package-lock.json`

## Commands

Run commands from `BA_Web/`:

```sh
npm install
npm run build
npm run dev
```

## Branch Boundary

Do not add mailing or React Email work to this branch. Mailing work belongs in the `codex/ba-mailing` worktree at:

```text
/Users/cfs-andres/Workspace/MetricLogic/ventures/automize/clients/BA
```

